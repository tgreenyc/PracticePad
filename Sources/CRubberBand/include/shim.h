// Thin umbrella header so the module has a stable header to include
// regardless of where Homebrew puts rubberband-c.h. The include path itself
// (-I .../include) is passed by the PracticePad target's unsafeFlags.
#include <rubberband/rubberband-c.h>
